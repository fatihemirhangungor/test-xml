foreach (XmlNode Node in list)
{
    // YENİ SAYFA OKU
    if (txtlblSecilenEftDosya.Text.ToUpper().Contains("EFT"))
    {
        using (var stream = new FileStream(sFileXlsEft, FileMode.Open, FileAccess.Read))
        {
            IWorkbook workbook = new XSSFWorkbook(stream);
            ISheet sheet = workbook.GetSheetAt(0);
            DataTable dtEft = new DataTable();
            
            bool firstRow = true;
            foreach (IRow row in sheet)
            {
                if (firstRow)
                {
                    foreach (ICell cell in row.Cells)
                    {
                        dtEft.Columns.Add(cell.StringCellValue);
                    }
                    firstRow = false;
                }
                else
                {
                    var dataRow = dtEft.NewRow();
                    int i = 0;
                    foreach (ICell cell in row)
                    {
                        dataRow[i] = cell.StringCellValue;
                        i++;
                    }
                    dtEft.Rows.Add(dataRow);
                }
            }
        }
    }

    if (txtlblSecilenHvlDosya.Text.ToUpper().Contains("HAVALE"))
    {
        using (var stream = new FileStream(sFileXlsHavale, FileMode.Open, FileAccess.Read))
        {
            IWorkbook workbook = new XSSFWorkbook(stream);
            ISheet sheet = workbook.GetSheetAt(0);
            DataTable dtHavale = new DataTable();
            
            bool firstRow = true;
            foreach (IRow row in sheet)
            {
                if (firstRow)
                {
                    foreach (ICell cell in row.Cells)
                    {
                        dtHavale.Columns.Add(cell.StringCellValue);
                    }
                    firstRow = false;
                }
                else
                {
                    var dataRow = dtHavale.NewRow();
                    int i = 0;
                    foreach (ICell cell in row)
                    {
                        dataRow[i] = cell.StringCellValue;
                        i++;
                    }
                    dtHavale.Rows.Add(dataRow);
                }
            }
        }
    }
}
